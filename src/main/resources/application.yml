server:
  port: 8880

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/slotgame
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: bx2BBKIjxQoE1GFixzPa0IYIl8+QQsYoLpafbJpRycs=
  access-token-expiration-ms: 36000000
  refresh-token-expiration-ms: 6048000000

logging:
  level:
    org:
      springframework:
        web: DEBUG
        security: DEBUG